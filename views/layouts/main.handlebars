<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>the tech blog</title>
  <link rel="stylesheet" href="/css/jass.css">
  <link rel="stylesheet" href="/css/style.css">
</head>

<body>
  <div class="flex-column justify-space-around">
    <header id="header" class="display-flex justify-space-between align-center p-2">

      <nav class="navigation">
        

        <a class = "nav" href="/">home</a>
        <a class = "nav" href="/profile">dashboard</a>
        {{!-- conditional --}}
        {{#if logged_in}}
        <button class="no-button" id="logout">logout</button>
        {{else}}
        <a class = "nav" href="/login">login</a>
        {{/if}}
        </class>
        <class="container container-fluid mt-5">
          <h1 id ="titlebar">
            <a id = "blogName" href="/">The Tech Blog</a>
          </h1>
        </class>


      </nav>

    </header>
    <main class="container container-fluid mt-5">
      <!-- Render the sub layout -->
      {{{ body }}}
    </main>
    <footer>

    </footer>
  </div>

  <!-- Render script for logged in users only -->
  {{#if logged_in}}
  <script src="/js/logout.js"></script>
  {{/if}}
</body>

</html>